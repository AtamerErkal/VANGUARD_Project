services:
  - type: web
    name: vanguard-ai-backend
    runtime: python
    plan: free
    pythonVersion: "3.11.9"
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python run_server.py
    envVars:
      - key: RENDER
        value: "true"
    healthCheckPath: /health
